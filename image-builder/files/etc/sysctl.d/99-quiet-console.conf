# console=tty1 stays so the boot log renders on the TV; sysctl.d applies once
# it already has, and from then on only a panic may repaint /dev/fb0 over the
# idle menu or a mirroring session. Everything else stays in dmesg/the journal.
kernel.printk = 1 4 1 7
