# uxplay's -efifo event channel, created here rather than by either process:
# uxplay runs as the uxplay user and cannot create anything in /run itself,
# and this inode has to outlive both units' restarts.
p /run/uxplay-events.fifo 0660 root uxplay -
