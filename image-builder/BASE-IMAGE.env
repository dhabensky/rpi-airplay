# PLACEHOLDER -- not yet populated. Run `make refresh-base-image` once
# (requires `gh auth login`; see image-builder/refresh-base-image.sh for
# the full rationale and what that script does) to populate this file for
# real, then commit it. Auto-regenerated by that script from then on --
# don't hand-edit the real values once it's populated.
BASE_IMAGE_URL=""
BASE_IMAGE_SHA256=""
BASE_IMAGE_DIETPI_VERSION=""
